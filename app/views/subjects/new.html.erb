<h1>New subject</h1>

<% form_for(@subject, :url => user_subjects_path, :method => "post") do |f| %>
  <%= f.error_messages %>
  
  <p><%= label_tag 'title' %><br/>
  <%= f.text_field :title %></p>

  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', user_subjects_path %>