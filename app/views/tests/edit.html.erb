<h1>Editing test</h1>

<% form_for(@test, :url => user_subject_test_path(@teacher, @subject, @test), :method => "put") do |f| %>
  <%= render "form", :f => f %>      
<% end %>

<%= link_to 'Back', user_subject_tests_path %>